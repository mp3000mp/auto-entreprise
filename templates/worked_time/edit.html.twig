{#{% extends 'layout/logged.html.twig' %}

{% block body %}#}
    <h1>{{ 'entity.WorkedTime.action.edit'|trans }}</h1>

    {{ form_start(form, {'action': path('worked_time.edit', {'id': workedTime.id})}) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary" type="submit">{{ 'form.edit'|trans }}</button>
    {{ form_end(form) }}
{#
{% endblock %}
#}
