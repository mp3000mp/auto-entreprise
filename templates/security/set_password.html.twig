{% extends 'layout/logged.html.twig' %}

{% block title %}Login{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-4 col-md-3 col-sm-2"></div>
        <div class="col-lg-4 col-md-6 col-sm-8">
            <h2 class="my-5">{{ 'security.password.reset'|trans }}</h2>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="float-right btn btn-primary">
                {{ 'security.password.reset'|trans }} <i class="fa fa-sign-in-alt"></i>
            </button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
