{% extends 'layout/security.html.twig' %}

{% block content %}
    {{ parent() }}

    <div class="text-center">

        <h2 class="my-5">{{ 'security.password.new'|trans }}</h2>

        {#
        {% if message.text != '' %}
            <div class="my-5">
                <span class="alert alert-{{ message.context }}">
                    {{ message.text|trans }}
                </span>
            </div>
        {% endif %}
        #}

        <div class="row">
            <div class="col-lg-4 col-md-3 col-sm-2"></div>
            <div class="col-lg-4 col-md-6 col-sm-8">

                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}

            </div>
        </div>

    </div>

{% endblock %}
