{#{% extends 'layout/logged.html.twig' %}

{% block body %}#}
    <h1>{{ 'entity.Cost.action.new'|trans }}</h1>

    {{ form_start(form, {'action': path('cost.new')}) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary" type="submit">{{ 'form.create'|trans }}</button>
    {{ form_end(form) }}

{#
{% endblock %}
#}
